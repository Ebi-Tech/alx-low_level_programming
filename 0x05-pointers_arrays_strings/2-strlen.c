#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: character to check
 * Return: length
 */

int _strlen(char *s)
{
	size_t length = 0;

	while (*str++)
		length++;

	return (length);
}
